import { Compass, Heart, Home, Library } from 'lucide-react';
import { motion } from 'framer-motion';
const items = [{ icon: Home, label: 'Trang chủ', id: 'home' }, { icon: Compass, label: 'Khám phá', id: 'discover' }, { icon: Library, label: 'Thư viện', id: 'library' }, { icon: Heart, label: 'Yêu thích', id: 'favorites' }];
export function MobileNavigation({ activeView, onNavigate }: { activeView: string; onNavigate: (view: string) => void }) { return <motion.nav className="mobile-nav glass" aria-label="Điều hướng chính" initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: .35, ease: [0.22, 1, 0.36, 1] }}>{items.map(({ icon: Icon, label, id }) => { const active = activeView === id; return <motion.button type="button" aria-label={label} aria-current={active ? 'page' : undefined} className={active ? 'active' : ''} whileTap={{ scale: .92 }} transition={{ type: 'spring', stiffness: 520, damping: 32 }} onClick={() => onNavigate(id)} key={id}>{active && <motion.i className="mobile-active-pill" layoutId="mobile-active-pill" transition={{ type: 'spring', stiffness: 420, damping: 34 }} />}<Icon /><span>{label}</span></motion.button>; })}</motion.nav>; }
